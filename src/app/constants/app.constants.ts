export const OPEN_HOUR_FOR_SELECTOR: number[] = Array.from(
  { length: 11 },
  (_, i) => i + 8
);

export const MINUTES_FOR_SELECTOR: number[] = Array.from(
  { length: 12 },
  (_, i) => i * 5
);

export const HORAIRES_OUVERTURE = '08:00';
export const HORAIRES_FERMETURE = '18:45';
